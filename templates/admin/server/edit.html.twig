{% extends 'base.html.twig' %}

{% block title %}Edit Server{% endblock %}

{% block main %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h1>Edit Server</h1>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="card">
            {{ include('admin_server/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('admin_server_index') }}">back to list</a>

            {{ include('admin_server/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
