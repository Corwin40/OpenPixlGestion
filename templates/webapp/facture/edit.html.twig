{% extends 'base.html.twig' %}

{% block title %}Edit Facture{% endblock %}

{% block body %}
    <h1>Edit Facture</h1>

    {{ include('webapp_facture/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('webapp_facture_index') }}">back to list</a>

    {{ include('webapp_facture/_delete_form.html.twig') }}
{% endblock %}
